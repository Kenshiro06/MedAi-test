[phases.setup]
nixPkgs = ['python310', 'python310Packages.pip', 'gcc', 'pkg-config']

[phases.install]
cmds = ['chmod +x install-deps.sh', './install-deps.sh']

[phases.build]
cmds = ['echo "Python Flask API build complete"']

[start]
cmd = 'python3 malaria_api_gradcam.py'

[variables]
PYTHON_UNBUFFERED = "1"
FLASK_ENV = "production"