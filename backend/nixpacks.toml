[phases.setup]
nixPkgs = ['python310', 'python310Packages.pip']

[phases.install]
cmds = ['python railway-start.py']

[phases.build]
cmds = ['echo "✅ MedAI Backend ready"']

[start]
cmd = 'python malaria_api_gradcam.py'