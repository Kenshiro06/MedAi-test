# Nixpacks configuration for Railway deployment
# Simple and clean - just Python Flask API

[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["pip3 install -r requirements.txt"]

[start]
cmd = "python3 malaria_api_gradcam.py"
