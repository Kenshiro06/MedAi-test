services:
  - type: web
    name: medai-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python malaria_api_FINAL_WORKING.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
      - key: PORT
        generateValue: true
