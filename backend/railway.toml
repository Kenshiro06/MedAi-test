# Railway Configuration - Nixpacks Build
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python3 malaria_api_gradcam.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[variables]
PYTHON_UNBUFFERED = "1"
FLASK_ENV = "production"
PORT = "8080"